<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    
    <?php
    // count(): Đếm số phần tử trong mảng 0r do dai cua mang
    // $numbers = array(1, 2, 3, 4, 5);
    // $count = count($numbers); // $count = 5
    // echo " " . $count;

    // array_unshift(): Thêm một hoặc nhiều phần tử vào đầu mảng.
    // $names = array("cam", "xoai");
    // array_unshift($names, "tao", "chanh"); 
    // print_r($names);

    // array_push(): Thêm một hoặc nhiều phần tử vào cuối mảng.
    // $names = array("tao", "chanh");
    // array_push($names, "cam", "xoai");
    // print_r($names);

    // array_shift(): Lấy ra phần tử đầu tiên của mảng
    // $names = array("tao", "chanh", "cam", "xoai");
    // $first_name = array_shift($names);
    // print_r($first_name);

    // array_pop(): Lấy ra phần tử cuối cùng của mảng.
    // $names = array("tao", "chanh", "cam", "xoai");
    // $last_name = array_pop($names);
    // print_r($last_name);
    
    // array_slice(): Trích xuất một phần của mảng.
    // $names = array("tao", "chanh", "cam", "xoai");
    // $slice = array_slice($names, 1, 2); 
    // print_r($slice);

    // array_splice(): Xóa hoặc thay thế một phần của mảng.
    // $names = array("tao", "chanh", "cam", "xoai");
    // array_splice($names, 1, 2, array("nho", "kiwi")); 
    // print_r($names);


    
    // array_reverse(): Đảo ngược thứ tự các phần tử trong mảng.
    // $numbers = array(1, 2, 3, 4, 5);
    // $reversed_numbers = array_reverse($numbers); 
    // print_r($reversed_numbers);
    
    // sort(): Sắp xếp các phần tử trong mảng theo thứ tự tăng dần.
    // $numbers = array(5, 3, 1, 4, 2);
    // sort($numbers); 
    // print_r($numbers);
    
    // rsort(): Sắp xếp các phần tử trong mảng theo thứ tự giảm dần.
    // $numbers = array(5, 3, 1, 4, 2);
    // rsort($numbers); 
    // print_r($numbers);


    ?>
</body>
</html>